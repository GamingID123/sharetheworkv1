#!/usr/bin/env sh
# Stub gradlew for CI — real wrapper is generated by Android Studio; this delegates to gradle if installed
set -e
if [ -f "./gradlew.bat" ]; then echo "gradlew stub: delegating to gradle"; fi
exec gradle "$@"
